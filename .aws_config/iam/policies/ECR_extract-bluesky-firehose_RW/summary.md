# Policy name
`ECR_extract-bluesky-firehose_RW`

# Description

This policy grants the bearer with read-write access to the Elastic Container Registry repo called "extract-bluesky-firehose". It should also allow the bearer to update this repo, and use `ecr:GetAuthorizationToken`.